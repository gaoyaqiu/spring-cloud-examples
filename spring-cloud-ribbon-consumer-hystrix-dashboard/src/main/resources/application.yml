server:
  port: 9004
  compression:
    enabled: true

spring:
  application:
    name: ribbon-consumer-hystrix-dashborad

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
    healthcheck:
      # 一定要启用健康检查
      enabled: true
